---
import certificates from '../data/certificates';
---
<section id="credentials" class="max-w-5xl mx-auto p-6 mt-6 bg-slate-900/60 border border-slate-800 rounded-lg">
  <h2 class="text-xl font-semibold">Certificate Credentials (preview)</h2>
  <pre class="mt-4 bg-slate-950 p-3 rounded text-sm text-slate-300 overflow-auto">
{certificates.map(c => `${c.title} | ${c.issuer} | Issued: ${c.issued}`).join('\n')}
  </pre>
</section>